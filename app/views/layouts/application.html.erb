<!DOCTYPE html>
<html>
  <head>
    <title>Plantagua</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class = "d-flex flex-column vh-100 <%= set_background_class(params) %>">
   <%= render "shared/flashes" %>

    <div class = "main-container <%= "container " unless set_background_class(params) == "leaves" %>">
      <%= yield %>
    </div>
  </body>
</html>
<!--"white-hover" if controller_name == 'sessions') || ("white-solid" if 'users/edit')>
